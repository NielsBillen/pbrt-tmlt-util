package task;

/**
 * 
 * @author Niels Billen
 * @version 0.1
 * 
 */
public class RenderTaskProgressAdapter implements RenderTaskProgressListener {
	/*
	 * (non-Javadoc)
	 * 
	 * @see task.RenderTaskProgressListener#completion(double, double, double)
	 */
	@Override
	public void completion(double percentage, double elapsed, double eta) {
	}
}
